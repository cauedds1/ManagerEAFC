{
  "$schema": "https://schema.railway.app/railway-config-schema.json",
  "build": {
    "buildCommand": "pnpm install --frozen-lockfile && BASE_PATH=/ pnpm --filter @workspace/fc-career-manager run build && mkdir -p artifacts/api-server/public && cp -r artifacts/fc-career-manager/dist/public/. artifacts/api-server/public/ && pnpm --filter @workspace/api-server run build"
  },
  "deploy": {
    "startCommand": "node --enable-source-maps artifacts/api-server/dist/index.mjs",
    "restartPolicyType": "ON_FAILURE",
    "restartPolicyMaxRetries": 10
  }
}
